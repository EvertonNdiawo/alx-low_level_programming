#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point of code.
 *
 * Return: 0 always a success.
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}

	putchar('\n');
	return (0);
}

