In this directory, I am going to work with a static library that I will create based on previously prepared functions.

libmy.a -> a static library that contains several object files in it.

create_static_lib.sh -> a script that creates a static library called liball.a from all the .c files in a current directory.
