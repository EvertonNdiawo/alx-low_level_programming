This project explores more on loops, nested loops to be specific and functions in the c programming language
All the programs are listed below and their functionality.

0-putchar.c -> A c program that returns "_putchar" to the standart output and only using the putchar function to do this.

1-alphabet.c -> program that prints the alphabet in lowercase and finishes off with a newline character.Can only use _putchar function for writing to standard output.

2-print_alphabet_10x.c -> A program that prints the alphabet in lowercase ten times followed by a newline.

3-
