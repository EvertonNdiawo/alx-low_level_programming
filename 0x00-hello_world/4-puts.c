#include <stdio.h>

/**
 * main - Where execution begins
 *
 * Description: A program that prints to the standard out
 * Return:0 succes, -1 fail.
 */

int main(void)
{
	char *str = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
